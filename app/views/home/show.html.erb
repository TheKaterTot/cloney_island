<div class="row">
  <div class="col s12 m12">
    <div class="card-panel blue lighten-5">
      <span class="center black-text">
        <h4>Join the Stack Overflow Community</h4><br>
        <p>Stack Overflow is a community of 6.9 million programmers, just like you, helping each other.</p>
        <p>Join them; it only takes a minute:</p>
        <div class="center"><a class="waves-effect blue darken-1 waves-light btn-large">Sign Up</a></div>
      </span>
    </div>
  </div>
</div>

<div class="row">
  <div class="col s12 m4 l3">
    <div class="row">
      <div class="col s12 m12">
        <div class="card white">
          <div class="card-content black-text">
            <span class="card-title">Categories</span>
            <% @presenter.all_categories.each do |category| %>
            <a class="waves-effect blue darken-1 waves-light btn-large"><%= category.name %></a>
            <br>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="col s12 m8 l9">
    <div class="row">
      <div class="col s12 m12">
        <div class="card white">
          <div class="card-content black-text">
            <span class="card-title">Recent Activity</span>
            <div class="collection">
              <% @presenter.recent_questions.each do |question| %>
              <a href="#!" class="collection-item"><%= question.title %></a>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
