<div class="right col s12 m12">
  <div class="card-panel" id='question_comments'>
    <span class="blue-text">
      <% @question.comments.each do |comment| %>
        <%= comment.body %><br>
      <% end %>
    </span>
  </div>
</div>
